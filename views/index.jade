extends layout

block content
  h1= title
  p Welcome to #{title}

  ul 
    each msg in messages
      li
        p=msg.text 
        p by #{msg.user}
        p on #{msg.added}

  a(href="/new") Add a new message